{
  "name": "team-lion-app",
  "version": "1.0.0",
  "description": "Full-stack app for Team Lion",
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "echo \"Nothing to build for the frontend.\"",
    "build:backend": "cd backend && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
